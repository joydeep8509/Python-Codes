# Polymorphism means same function name (but different signatureS)being uses for different types.
# interpritetion(EarlyBinding) ->function over loading ,operator over loading, constructor(not supported) over loading
# runTimePolymorphism(late-binding )->overrinding(functions)

l=[10,20,30,40]
print(len(l))
s="Welcome"
print(len(s))